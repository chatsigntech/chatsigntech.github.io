window.CHATSIGN_CONFIG = Object.assign({}, window.CHATSIGN_CONFIG, {
  // The GitHub Pages maintainer may place the Web3Forms access key here, or
  // provide VITE_WEB3FORMS_ACCESS_KEY while building the source project.
  web3formsAccessKey: '',
})
